apiVersion: v1
kind: Service
metadata:
  name: simple-app-service
spec:
  type: NodePort
  selector:
    app: simple-app
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30001  # Accessible via <node-ip>:30001